<?xml version="1.0" encoding="UTF-8"?>
<!--
  PI E-709.CRG + P-621.1CD 的设备认识账
  用途：这台装置的接入方式、实测数字、动作语义与踩坑经验。需求写 AGENTS.md，不重复。
  证据强度：实测（上机量到）｜文档互证（与厂家手册一致）｜推断（仅自洽，未直接验证）
-->
<设备认识账 控制器="PI E-709.CRG" 台子="P-621.1CD" 日期="2026-09-17">

  <已证实>
    <条目 编号="A1" 主题="接入方式" 证据强度="实测+文档互证">
      <结论>Windows 与 Linux 两条通路，都走 PIPython，但网关不同。</结论>
      <证据>Windows：装 PI Software Suite 2.8.2.0（提供 GCS DLL 与 USB 驱动），走 ConnectUSB。Linux：**不需要装任何 PI 软件**——内核 ftdi_sio 直接提供 FTDI 虚拟串口，PIPython 以 PISerial 走纯 Python 命令层，不加载 GCS DLL（115200）。</证据>
      <证据>设备同一时刻只能有一个占用者：用本仓库之前必须关掉 PIMikroMove。</证据>
      <处置>接入细节与环境变量（PI_LINK / PI_SERIAL_PORT / PI_SERIAL_BAUD / PI_SERIAL）见仓库根 README.md。</处置>
    </条目>

    <条目 编号="A2" 主题="停稳判据" 证据强度="实测">
      <结论>停稳判据由控制器给出：到位信号置位后，再加一段固定延时（默认 100 ms，界面上可调）。</结论>
      <处置>判据在设备层实现，上层只处理「到位 / 超时 / 中止」。</处置>
    </条目>

    <条目 编号="A3" 主题="方向性迟滞" 证据强度="实测">
      <结论>方向性迟滞约 0.1 µm。</结论>
      <处置>扫描必须单向逼近，往返走会因迟滞导致图像错位。</处置>
    </条目>

    <条目 编号="A4" 主题="停止" 证据强度="实测">
      <结论>能立刻停住并保持位置；伺服不动、不漂移。</结论>
      <处置>后端 StopResult 报 HARD。</处置>
    </条目>

    <条目 编号="A5" 主题="释放（卸力）" 证据强度="实测">
      <结论>能卸掉输出。释放后控制器拒绝运动，恢复时原地保持。</结论>
      <证据>SVO 1 会把当前位置写进目标寄存器，因此恢复时不产生位移——已实测验证。</证据>
      <结论>例外：当前位置已在标定行程 [TMN?, TMX?] 之外时，目标会被夹到行程端点，恢复动作会把台子拉回行程内。</结论>
      <证据>连接 / 重连本身即可能触发，不限于用户从「释放」恢复。</证据>
      <结论>关伺服后位移台会回弹到静止位（定位仪器的正常状态）。</结论>
      <处置>异常振动时使用；界面文案按实际语义写，不写「原地保持」以外的承诺。</处置>
    </条目>

    <条目 编号="A6" 主题="行程上下限" 证据强度="实测">
      <结论>行程上下限由控制器标定给出（TMN? / TMX?），是软限位的权威来源。</结论>
      <处置>连接时读回，不写死；手动移动与扫描起终点都按它夹取。</处置>
    </条目>

    <条目 编号="A7" 主题="已知坑" 证据强度="实测">
      <结论>关闭连接后仍有回调打到已关闭的旧连接上，会报 PortNotOpenError。</结论>
      <证据>实测复现。</证据>
      <处置>连接生命周期的收尾要按此设防。</处置>
    </条目>
  </已证实>

  <未证实>
    <条目 编号="B1" 主题="绝对精度">
      <内容>本仓库没有对 PI 做过绝对精度/线性度的外部标定；报出的 µm 是控制器自己的读数。</内容>
      <影响>与 XMT 同：绝对精度要靠外部基准（干涉仪、显微镜刻度），不在本仓库范围内。</影响>
    </条目>
    <条目 编号="B2" 主题="长期漂移与长跑可靠性">
      <内容>长时间静止的漂移、以及扫描器相对校验的长期假失败率，未系统评估。</内容>
    </条目>
  </未证实>
</设备认识账>
