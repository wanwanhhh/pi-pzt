<?xml version="1.0" encoding="UTF-8"?>
<!--
  索雷博 CS165MU（Zelux，1.6 MP 单色 sCMOS）的设备认识账
  用途：这台相机的接入方式、实测数字、动作语义与踩坑经验。需求写 AGENTS.md，不重复。
  证据强度：实测（上机量到）｜文档互证（与厂家手册/SDK 源码一致）｜推断（仅自洽，未直接验证）
  配套代码：backend/thorlabs_ccd.py（设备层）、tools/hwtest_ccd.py（上机自检）、tools/thorlabs_probe.py（联通性探针）
-->
<设备认识账 控制器="索雷博 CS165MU（Zelux）" 台子="无（成像器件）" 接口="USB VID_1313 PID_4002，SDK thorlabs_tsi_sdk 0.0.8（ctypes 包原生 DLL）" 日期="2026-09-20">

  <已证实>
    <条目 编号="A1" 主题="接入方式与占用" 证据强度="实测+文档互证">
      <结论>Python 侧用厂家 SDK 包 thorlabs_tsi_sdk（0.0.8）。**PyPI 上没有这个包**，只能从厂家安装包解出来的
        SDK\Python Toolkit\thorlabs_tsi_camera_python_sdk_package.zip 用 pip 装；同目录 source\ 是不用安装的源码版。</结论>
      <证据>pip install thorlabs_tsi_sdk → "No matching distribution found"；从本地 zip 装成功（py3-none-any，纯 Python）；sha256 与 SDK 里 source/tl_camera.py 一致。</证据>
      <结论>SDK 只包 Python，**不含原生 DLL**；DLL 在 SDK\Python Toolkit\dlls\64_lib（19 个）。</结论>
      <结论>设备同一时刻只能有一个占用者：**ThorCam 必须关闭**，否则打不开。</结论>
      <结论>驱动是 Thorlabs 自己的 CYUSB3（1.2.3.14），不是系统 usbser。</结论>
      <证据>Get-PnpDevice：USB\VID_1313&amp;PID_4002\000000000AAA，Service=CYUSB3，Status=OK，ProblemCode=0。</证据>
      <处置>接入与依赖见仓库根 README.md；设备层见 backend/thorlabs_ccd.py。</处置>
    </条目>

    <条目 编号="A2" 主题="DLL 加载方式" 证据强度="实测">
      <结论>**必须在进程里按全路径把 19 个厂商 DLL 预加载一遍**（ctypes.WinDLL(全路径)）。</结论>
      <证据>只 os.add_dll_directory() + os.environ["PATH"] 前置 + 按短名加载 thorlabs_tsi_camera_sdk.dll，
        实测报 "Could not find module 'thorlabs_tsi_camera_sdk.dll' (or one of its dependencies)"；
        在**启动 Python 之前**把目录放进 PATH 能让它通过，但在 Python 进程内改 os.environ["PATH"] 无效
        （窗口已过），而且同一份代码在不同调用方式下表现不一致，极难复现。
        按全路径预加载后：干净 PATH 下 19/19 成功，相机正常打开。</证据>
      <处置>backend/thorlabs_ccd.py 的 _require_dll_dir() 里做预加载，**不依赖调用者先把目录塞进 PATH**。
        run_camera.bat 仍然设 PATH，那是给工具脚本兜底，不是后端的前提。</处置>
    </条目>

    <条目 编号="A3" 主题="像素格式与满量程" 证据强度="实测">
      <结论>镜头盖打开后、曝光拉到固件下限仍饱和的照明下，读出值**顶在 1022**（不是 4095）。</结论>
      <证据>40 µs 曝光（固件夹到的最小值）满帧 max=1022，且 52% 的像素等于 1022；长曝光同样停在 1022。</证据>
      <结论>所以"饱和"要按**实际最大值**判，拿 4095 当门限会把过曝帧判成"没饱和"。</结论>
      <证据>踩过：用 4095 做门限跑了一轮过曝测试，脚本一路报"饱和 0.00%"，白跑。</证据>
      <处置>tools/hwtest_ccd.py 的告警按实际最大值统计；预览 JPEG 用 >>2 降到 8 位（配合 1022 满量程，
        不是 >>4 —— 那是 65535 满量程的算法，会把 10 位信号白扔 4 位）。</处置>
    </条目>

    <条目 编号="A4" 主题="曝光线性与光源稳定性" 证据强度="实测">
      <结论>增益 0 下，画面对曝光**高度线性**，约 0.10 ADU/µs（全幅均值）。</结论>
      <证据>40/100/250/500/1000/2000/4000/8000 µs → 均值 5.4 / 10.4 / 25.4 / 50.6 / 101.6 / 201.4 / 402.5 / 805.1；
        隔 3 秒重复一轮，同曝光点重复性在 0.1 以内。</证据>
      <结论>固件曝光范围 40 µs ~ 26843432 µs；请求低于 40 µs 会被相机夹到 40。</结论>
      <结论>这台相机**没有自动曝光属性**（SDK 里 auto_exposure* 为空）。</结论>
      <注意>照明是会变的：同一套光路两小时后，8 ms 曝光的均值从 805 掉到约 337，再过一阵 15 ms 才到 468。
        默认曝光只能当起点，实测为准。</注意>
      <处置>**曝光预览与采图共用一个值**（TL_EXPOSURE_US，界面默认 12000 µs）：两边分开的话，
        "预览里看着挺好"和"存下来的"就是两张亮度不同的图，对不上账。界面上可实时改。
        **增益固定 0**（PI_CCD_GAIN，接口一律拒绝）：它只放大信号+噪声，不改善信噪比。</处置>
    </条目>

    <条目 编号="A5" 主题="曝光与增益掉的坑（都是掉电保持）" 证据强度="实测">
      <结论>**曝光和增益都是掉电保持的**：进程退出后相机记着上次的值，下一次连接不写就是上次那个。</结论>
      <证据>一个脚本把增益设成 480 档（拉满）后没有还原，后续几次运行都没再写增益，
        于是"40 µs 也过曝、100% 像素顶死"，被误判成"光太强、要加衰减片"，白折腾一轮。</证据>
      <结论>**软触发后至少等 300 ms 才能设曝光**（SDK 源码里写明的）。不等的话 set 不报错、固件不认账。</结论>
      <证据>tl_camera.py:918 的文档原话；实测不等时"要 8000 µs 读回 2011 µs"（预览的旧值）。</证据>
      <结论>**预览流跑着的时候可以不停流直接改曝光**，1~2 帧内亮度就变。</结论>
      <证据>set 耗时 3.4 ms、读回 1.6 ms；2000→8000 µs 后画面均值 62 → 248（正好 4 倍）。
        端到端（HTTP，服务不停）：15000/5000/30000/15000 µs → 均值 117.9 / 38.9 / 236.1 / 117.8。</证据>
      <结论>增益也是随时可设，不用停流。</结论>
      <注意>曝光可以实时改（预览与采图一起改）。但**每帧保存时记的必须是相机读回的实际值**，
        不是界面填的请求值 —— 实测请求 12000 µs 读回 11995 µs，差 5 µs 也要如实记。</注意>
      <处置>每次连接都**显式写增益与曝光并读回确认**（掉电保持，不写就是上次那个）。</处置>
    </条目>

    <条目 编号="A6" 主题="ROI 与帧率" 证据强度="实测">
      <结论>**ROI 会被相机按硬件对齐改写**（要 259×259 实际 260×260、要 518×518 实际 520×520、要 520×520 实际 528×528）。</结论>
      <处置>设完必须读回，一切记账用实际值。</处置>
      <结论>采集中改 ROI 会被固件拒绝。</结论>
      <证据>tl_camera_set_roi() → error code 1005 "Camera Running Error: Firmware-reported error"。只能先 disarm。</证据>
      <结论>帧率（只轮询、不处理）：全幅 1440×1080 → 34.8 fps；520×520 → 69.5 fps；260×260 → 129.3 fps。
        曝光从 5 ms 压到 1 ms 都是 34.8 fps，说明瓶颈在读出/传输，不在曝光。</结论>
      <结论>USB 带宽约 108 MB/s（全幅 3.11 MB × 34.8 fps）。</结论>
      <处置>预览默认用**原生全幅**（跟保存一致）：早先为速度设过左上角 520×520，
        结果预览只显示全幅的 17.9%，容易被误当成"视野就这么大"。</处置>
    </条目>

    <条目 编号="A7" 主题="单点采图的耗时构成" 证据强度="实测">
      <结论>从预览切到原生全幅采一帧并落盘，约 1.4 s。</结论>
      <证据>构成：disarm + 改 ROI + 300 ms 触发等待 + arm + 全幅读帧（约 70 ms，含曝光）+ PNG 编码落盘。</证据>
      <结论>16 位 PNG 编码落盘：全幅约 17~100 ms，1.3 MB；纯黑/纯平场帧编码最快（17 ms），有图案时慢些。</结论>
      <注意>保存的图是**原生 1440×1080、16 位、不裁剪、无位深损失**；预览的小窗口只影响预览。</注意>
    </条目>

    <条目 编号="A8" 主题="预览编码与节拍" 证据强度="实测">
      <结论>uint16 → 8 位 → JPEG 约 4~21 ms/帧；全幅 JPEG 20~90 KB。</结论>
      <证据>分段日志："预览 1440x1080：等帧 17 ms + 编码 4 ms = 21 ms/帧（23701 字节）"。</证据>
      <结论>界面按 10 fps 拉，实测到手 8~10 fps；**后端产帧 11.7 fps**（配置上限 15）。
        早先只有 7 fps，原因是队列超时与取帧闸门两段等待相加（50 + 66 ms）。</结论>
      <处置>预览节拍把"等到该取下一帧的时刻"直接当队列超时用，不要两段各写一个固定值。</处置>
    </条目>
  </已证实>

  <未证实>
    <条目 编号="B1" 主题="1022 与 12 位的关系">
      <内容>SDK 报"12 位 ADC"，但读出上限是 1022（约 10 位）。中间那两位要么是 SDK 的缩放、要么是传感器的黑电平/增益留白。</内容>
      <影响>换算成电子数时要弄清楚这个系数；只做显示与相对比较则不影响。</影响>
    </条目>
    <条目 编号="B2" 主题="绝对的线性度与绝对辐照度">
      <内容>只验证了"曝光—读数"的线性，没有对绝对辐照度标定，也没有测暗电流/热噪声随温度的变化。</内容>
      <影响>图像可以比相对亮度，不能直接当辐射度用。</影响>
    </条目>
    <条目 编号="B3" 主题="外部触发与同步">
      <内容>本机只用过软件触发（issue_software_trigger）。CS165MU 是否有外部触发/帧同步接口、能不能和位移台/光源硬同步，没验证。</内容>
      <影响>现在的"停稳 → 软件触发采图"完全够用（主流程本来就是逐点停稳），要硬同步再说。</影响>
    </条目>
    <条目 编号="B4" 主题="binning / taps / data_rate">
      <内容>未测。SDK 有 binx/biny、taps、data_rate 这些接口，但本机的场景（ROI + 全幅）用不上。</内容>
    </条目>
    <条目 编号="B5" 主题="长时间连续预览的稳定性">
      <内容>连续跑过一个多小时（帧计数 900+）没出问题，但没有做过夜的稳定性与掉线恢复测试。</内容>
    </条目>
  </未证实>

  <已作废>
    <条目 编号="C1">「这台相机在最小曝光 40 µs 下仍然过曝，说明光太强、要加衰减片」——**错的**。当时的"过曝"是
      增益被上一个脚本留在 480 档（掉电保持）造成的。增益归 0 后 8 ms 曝光均值约 700/1022，余量充足，
      光路不需要衰减。（结论作废，但留下的教训是真的：见 E3。）</条目>
    <条目 编号="C2">「tl_camera_open_sdk() 返回 error code 1 是因为 SDK 被上一个进程占着、要等锁释放」——**不准确**。
      error 1 的真实触发是 DLL 加载路径不对（见 A2）。"等一会儿又好了"是 PATH 环境不同造成的巧合。
      后来还发现厂家包装层 dispose() 其实**会**调 tl_camera_close_sdk()，所以"dispose 有 bug"那条也不成立。</条目>
    <条目 编号="C3">「预览用小 ROI（左上角 520×520）更好，因为只有全幅 18% 的数据」——速度上确实更快
      （69.5 fps vs 34.8 fps），但**容易被误认为视野就这么大**。默认改成原生全幅；
      仍保留 TL_PREVIEW_ROI 作为需要时的加速手段。</条目>
  </已作废>

  <经验与坑>
    <条目 编号="E1" 主题="DLL 必须按全路径预加载">
      <内容>按短名加载失败、按 PATH 加载时好时坏，排查花了两轮对照实验。</内容>
      <处置>见 A2；tools/thorlabs_probe.py 会在报错时把"缺哪个 DLL"逐条打出来。</处置>
    </条目>
    <条目 编号="E2" 主题="error code 1 是一句会误导人的话">
      <内容>SDK 的这句错误提示指向"SDK 已被占用"，实际根因是 DLL 加载。</内容>
      <处置>设备层把"找不到 DLL / 不在 PATH"做成**启动前就显式拒绝并说明原因**，不让上层去猜错误码。</处置>
    </条目>
    <条目 编号="E3" 主题="掉电保持的参数会伪装成光学问题">
      <内容>增益 480 档被留在了相机里，表现却是"光太强"。</内容>
      <处置>连接时显式写增益/曝光并读回；增益固定在 0（界面不给），因为它只放大信号+噪声、信噪比不变。</处置>
    </条目>
    <条目 编号="E4" 主题="触发之后 300 ms 内不许设曝光">
      <内容>set 不报错，但固件不认账，读回还是旧值。</内容>
      <处置>_apply() 里按 _last_trigger 补足 300 ms；预览中的实时微调走"不停流直接改"那条路（不受这条限制）。</处置>
    </条目>
    <条目 编号="E5" 主题="饱和门限不能想当然">
      <内容>满量程是 1022，不是 4095。</内容>
      <处置>按实际最大值判饱和；脚本与界面都别写死 4095。</处置>
    </条目>
    <条目 编号="E6" 主题="开关一次就要重新枚举 USB">
      <内容>每次 TLCameraSDK()/dispose() 都会让 CYUSB3 重新枚举设备，Windows 会"叮咚"一声；
        短时间内另一个进程再开会撞上 error code 1。连续开关多次时用户会听到一串连接/断开音。</内容>
      <证据>设备 LastArrivalDate 在脚本反复开关的几分钟里连跳数次；不碰 SDK 时纹丝不动。</证据>
      <处置>**SDK 进程内只开一次、退出前只关一次**：预展开一次、采图跟着预展、服务退出时 close()。
        前端停预览只停止取帧，不关相机。</处置>
    </条目>
    <条目 编号="E7" 主题="16 位 PNG 喂 PIL 会抛">
      <内容>PIL 对 16 位灰度 PNG 的 thumbnail()/point() 都不支持（L;16 没有对应实现）。</内容>
      <处置>先读进 numpy、>>2 降到 8 位、再交给 PIL；16 位写入用自带的 zlib 实现（tools/png16.py 与 backend 里各一份，后端不 import tools/）。</处置>
    </条目>
    <条目 编号="E8" 主题="接口线程安全">
      <内容>SDK 不是线程安全的，且一个进程只能有一个 SDK 实例。</内容>
      <处置>一个 owner 线程 + 任务队列，开/关/改设置/取帧全排队；与位移台的 owner 线程是同一条规矩。</处置>
    </条目>
  </经验与坑>

  <对后端的处置清单>
    <事项 优先级="P0" 状态="已完成 2026-09-20">DLL 按全路径预加载 19 个厂商库（backend/thorlabs_ccd.py:_require_dll_dir）。
      复核：干净 PATH（不含 DLL 目录）下服务能打开相机、出预览帧。</事项>
    <事项 优先级="P0" 状态="已完成 2026-09-20">SDK 进程内只开一次、退出前只关一次（server lifespan 里 close()）；
      每点采图不重开相机。复核：连开 8 次不重试全成功、连续预览一个多小时不掉。</事项>
    <事项 优先级="P0" 状态="已完成 2026-09-20">连接时显式写增益/曝光并读回（掉电保持，不写就是上次那个）。
      复核：采图帧的元数据里曝光读到 8001 µs（请求 8000），与直接设 8000 的对照帧均值一致（670.4 vs 670.6）。</事项>
    <事项 优先级="P0" 状态="已完成 2026-09-20">曝光可实时调，**预览与采图共用**（/api/ccd/exposure 一次改两边）；
      增益固定 0 且接口拒绝（/api/ccd/gain → 400）。复核：改 6 ms 后预览均值 46、改回 12 ms 后保存的帧
      读回 11995 µs；增益接口返回"固定为 0"。</事项>
    <事项 优先级="P0" 状态="已完成 2026-09-20">**曝光写进图像自己身上**：保存的 PNG 带 tEXt 块
      （键名 ExposureUs，值为相机读回的实际 µs），图库列表从文件里读出来显示（不落库）。
      复核：保存后 read_png_meta() 读回 11995；老图（本次改动之前存的）显示"曝光未记录"。</事项>
    <事项 优先级="P0" 状态="已完成 2026-09-20">保存一律**原生全幅、16 位 PNG、不裁剪**；预览 ROI 只影响预览。
      复核：/api/ccd/capture 返回 1440×1080、exposure_us=8001、roi=[0,0,1440,1080]。</事项>
    <事项 优先级="P0" 状态="已完成 2026-09-20">相机与扫描互斥：扫描运行中预览/采图一律 409（真正的拒绝在后端）；
      点「开始扫描」时界面先自行关预览。复核：扫描中调 /api/ccd/preview 返回 409。</事项>
    <事项 优先级="P1" 状态="已完成 2026-09-20">增益固定 0，界面不提供增益输入（只显示读数），避免手滑重演 C1；
      后端能力保留（/api/ccd/gain + PI_CCD_GAIN）。</事项>
    <事项 优先级="P1" 状态="已完成 2026-09-20">手动保存的原始帧与扫描各点的图**分开**：图库只列
      **库里登记过**的手动保存帧（登记只发生在 save_raw 这条路上，靠库不靠文件名前缀猜），
      扫描图留在各自扫描的点位表里。复核：扫描采的图一张都没出现在 /api/grabs 里。</事项>
    <事项 优先级="P1" 状态="已完成 2026-09-20">原始帧可改名，**改的是磁盘上的文件名**（库记录同步改，失败回滚），
      可单删；另有 /api/grabs/purge?keep=N 批量只留最近 N 张，用于收尾清理。复核：
      "只留最近 1 张"删掉 7 张，文件、记录、缩略图缓存一起清掉。</事项>
    <事项 优先级="P1" 状态="已作废 2026-09-20">曾加过"给一帧写备注"（grab.note 列 + /api/grabs/note），
      但界面一直没有入口，等于库里留了一个没人写也没人读的字段。**已整条删除**：
      表里的 label/note 列、迁移项、set_grab_note、接口、/api/grabs 的返回字段一起清掉。
      要备注就等真做界面时再加，不做就不留半截。</事项>
    <事项 优先级="P1" 状态="已完成 2026-09-20">预览上显示**全局质心**（整幅图的强度加权重心）：
      口径由用户定为**不做任何处理** —— 不扣背景、不设阈值、不开窗；后端在原生 16 位帧上算完再下发
      （前端只画十字线），走 /api/ccd/status 的 centroid 字段。实现用"先按列/按行求和再除一次"
      （int64 精确，等价于 ΣI·x/ΣI，与 mgrid 浮点暴力法逐位一致）。实测：整幅 1440×1080
      **2.5 ms/帧**（预览编码从约 3 ms 变成约 6 ms，仍是 15 fps 预算的零头）。
      饱和计数用 TL_SATURATION_ADU = 1022（A3 的实测满量程）。
      **已知代价（用户已知情并选择该口径）**：6 px 的小光斑放在均值 400 ADU 的整幅背景上只占总强度
      0.01%，光斑走 1 px 全局质心只动 0.0001 px；亮 10 倍也只到 0.0011 px/px。要看小光斑就得改口径
      （扣背景/开窗/设阈值），改之前先看这个数。</事项>
    <事项 优先级="P1" 状态="已完成 2026-09-20">**预览可转朝向**（0/90/180/270 顺时针，界面上一次一档、
      跑一圈回到 0）：只转预览，用 np.rot90 的**视图**（精确置换、不重采样、不复制）。
      注意方向：np.rot90 默认是逆时针，界面要顺时针，所以传 -k —— 由测试钉住
      （左上角 (5,3) 转 90° 落到右上角）。
      **保存的原生帧永远是传感器朝向**；**质心读数也永远是传感器坐标**（在未旋转的那一帧上算），
      只有十字线由前端按朝向换算到显示帧（映射表两边各有一份测试）。
      实测（同一场景）：0° 与 90° 的质心一致（715.72, 539.02 / 715.75, 538.99，差值是帧噪声）、
      尺寸都报 1440×1080；而预览 JPEG 确实转了（1440×1080 → 1080×1440）；
      旋转状态下采的帧仍是 1440×1080；四档以外拒绝（45° → HTTP 400）。默认 PI_CCD_ROTATION=0。
      环境变量写错（如 45）时**只退成 0 并打 error 日志**，不让相机对象建不起来 —— 一个显示偏好
      不该把 /api/ccd/status 变成一直 500（评审提的，已改）。</事项>
    <事项 优先级="P1" 状态="定案 2026-09-20">扫描中相机出错（取帧超时 / SDK 报错）= **整条扫描 failed 就地停下**：
      不自动重试、不判"该点无效后继续"、不自动清理半截扫描。相机取帧本身会等满 TL_CAPTURE_TIMEOUT_S
      才放弃，真报错时多半不是瞬时抖动；相机真掉线时继续跑只会白采 + 白走行程。错误消息里带
      "第几点、目标多少"，点位表停在上一点；跑过的点都留在库里，**那条半截扫描由人在界面上手动删**
      （DELETE /api/scans/{id} 已有，且拒绝删正在跑的）。</事项>
    <事项 优先级="P1" 状态="已完成 2026-09-20">**点图取轮廓图**（用户要的）：在预览图上点一下，取过该点的
      **整行**（水平）与**整列**（垂直）两条**全长**剖面；图库大图点一下取保存的那个 PNG 的剖面。
      数据一律是**原生 16 位原值**（不平滑、不扣背景、不归一化）—— 预览 JPEG 已经 >>2 过、还有压缩伪影，
      拿它画剖面等于把伪影当信号，所以后端另留一份最近的原生帧（**必须 copy**：SDK 的 buffer
      下一轮就覆写，而剖面是 HTTP 线程随时来取的）。
      坐标是**显示坐标**：在旋转后的视图上切，所以转了 90° 之后"水平"仍是看到的水平（保存的文件不转，
      那条路就是文件坐标）。实测：朝向 0 时 1440/1080 点、朝向 90 时 1080/1440 点；
      响应 **10.1 KB / 4~6 ms**（~10 Hz 刷新 = 100 KB/s，可忽略）；**交叉点两条剖面同值**（777 = 777）；
      对保存的 PNG 与 numpy 直接切的行/列**逐位一致**。曾踩：SDK buffer 不 copy 会读到下一帧（数据竞争）。</事项>
    <事项 优先级="P1" 状态="已完成 2026-09-20">图库每格多一行**质心**（用户要的）：与曝光同一个做法 ——
      存盘时把质心写进 PNG 自己的 tEXt（键 CentroidPx，值 "cx,cy"，**传感器坐标**），
      列表从文件里读出来显示（read_png_meta 一次扫完曝光与质心，不落库）；**老图显示"质心未记录"**，
      不猜值。**扫描每点采的图走同一段保存代码，所以它们的文件里现在也带质心**
      （每点 +2.5 ms，相对约 1.4 s 的点周期可忽略）。复核：新帧 tEXt = [('ExposureUs','11995'), ('CentroidPx','751.60,560.44')]，
      与对同一文件重新算的质心 [751.60, 560.44] 一致（两位小数相同）。</事项>
    <事项 优先级="P1" 状态="已完成 2026-09-20">**16 位 PNG 直接给浏览器看是黑的**（用户报的"点开原始图片是黑的"）：
      我们存的是 16 位灰度，但值只占 0~1022（满量程 65535 的 1.6%）—— 浏览器按满量程渲染，
      实测均值 352 的帧在屏幕上只有 **1.4/255**。图库列表一直是走 >>2 的 JPEG 所以正常，
      而"点开看原图"那条路（大图）与**扫描页点位表的缩略图**都直接用了 /data/images/xxx.png，
      于是全黑。点位表那条尤其隐蔽：现有 163 张 scan 是**假相机产的 64×64 8 位**图，看着一直正常，
      跑一次真机扫描才会黑（评审抓到的漏网）。
      处置：这两条路都改走 >>2 的 JPEG（/api/grabs/thumb，大图 max_side=1440，与预览同一条口径），
      图下给一条**原始 PNG 的下载链接**（定量看文件本身，别指望浏览器）；
      **缩略图降到 8 位的口径按位深决定**：16 位才右移 2 位，8 位原样
      （一刀切右移会把假相机的 8 位占位图压暗 4 倍）。
      大图的说明条只有一个来源：图库里的帧用列表带回来的元数据，点位表的扫描帧现问
      /api/image/meta（同一段保存代码，扫描帧文件里也写着曝光与质心），两处都没有就写"未记录"，
      **绝不留上一张图的数字**。
      补记（第三批评审实测）：缩略图是"变小"而不是"变糊"—— Pillow 的 thumbnail() 本来就抗锯齿
      （reducing_gap=2 → 整数倍面积平均 reduce + BICUBIC），260 档只会把细结构抹平、不会摩尔纹；
      格子里的图之所以"看着跟预览不是一张"，是 CSS 的 image-rendering: pixelated（最近邻）干的：
      同一真帧缩到格子大小、对理想面积平均算 RMSE，最近邻 3.61 / 5.99 / 7.79（260/520/1440 档），
      平滑则都是 1.4~1.5 —— **源图越大、最近邻抽得越糟**。pixelated 已删；图库档位提到 520，
      那是给 DPR≥1.5 的余量（DPR=1 时 260 就够）。这帧的细结构周期主要在 24~48 px，不是 6~9 px。</事项>
    <事项 优先级="P2" 状态="待定">预览帧率上限：现在后端 15 fps、前端按 10 fps 拉（到手 8~10）。
      要更顺就调 TL_PREVIEW_FPS 与前端 CCD_LIVE_MS；全幅后端实测有余量到约 47 fps。
      **质心上来之后**前端每次取帧会多一个 GET /api/ccd/status（约 10 Hz，只读内存不碰设备）——
      请求数翻倍、无其它代价；真嫌吵可以把质心并进 SSE 或 preview.jpg 的响应头。</事项>
    <事项 优先级="P2" 状态="待定">docs/thorlabs/ 目前只有这份认识账；厂家 SDK 手册
      （Thorlabs_Camera_Python_API_Reference.chm）在 C:\Program Files\Thorlabs\Scientific Imaging\Documentation 下，未复制进仓库。</事项>
  </对后端的处置清单>

</设备认识账>
