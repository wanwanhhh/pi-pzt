# 直接依赖（版本为已在上机测试中验证过的组合）
# 上机验证过的两套环境：
#   Windows + Python 3.14 + PI Software Suite 2.8.2.0（GCS DLL 通道，ConnectUSB）
#   Linux（Ubuntu 24.04）+ Python 3.12 + FTDI 虚拟串口（PISerial，无需 PI 软件）
fastapi==0.141.1
uvicorn==0.53.0
pydantic==2.13.5
PIPython==2.13.0.2

# ---- 索雷博相机（可选；只有 PI_CCD=thorlabs 时才需要，仅 Windows）----
# thorlabs_tsi_sdk **不在 PyPI 上**（pip install 这个名字必然失败），要从厂家 SDK 包里装本地包；
# 原生 DLL 另用 TL_SDK_DLLS 指过去。**安装步骤见 README.md 的「相机」一节**，不在这里重复。
pillow==12.3.0
# numpy：相机那一路（backend/thorlabs_ccd.py，用到才 import）与 backend/tests/test_centroid.py 都要它。
# SDK 的本地包一般会把它带进来，但离线单测不该依赖「恰好装了相机」—— 不装就是 ModuleNotFoundError。
# 2.5.3 是在 Linux + Python 3.12 上跑通 test_centroid.py（18/18）的版本。
numpy==2.5.3
