# AGENTS.md

## 规则

1. 未经用户允许，不得创建子代理（subagent）。

## 需求

- 单人单机使用，界面中文。
- **跨平台**：同时兼容本机 Linux（Ubuntu 24.04）与 Windows。
- **后端负责控制**：硬件访问、参数校验、限位、安全逻辑、状态机全部在后端。
- **前端只负责显示与输入，不承担后端功能**：不复制业务逻辑、不做本地权威状态、不自己判断限位；前端被绕过或篡改也不影响设备安全。
- 主流程：**自动等间隔移动 → 停稳 → CCD 采图**，逐点进行。位置只作为每点元数据记录，**不做 10 kHz 高速采集通路**。
- 每点停稳判据：等 `ONT?` 置位后再加固定延时（默认 100 ms，界面可调）。
- 手动（点到点）与自动（扫描序列）都要；任务由后端执行器运行，**独立于浏览器连接**（关页面、断网都继续跑）。
- 扫描控制：**暂停 / 继续 / 中止** 三个独立动作。
- 扫描默认**单向逼近**：实测约 0.1 µm 方向性迟滞，往返走会导致图像错位。
- 停止分两个：「停止」= `STP` 保持伺服不动；「释放」= 关伺服（异常振动时用，手册明确建议）。
- **只做闭环**，不提供开环/电压模式；开环调试（频响、陷波滤波器）用 PIMikroMove。「释放」是状态不是模式：释放后拒绝运动（伺服关闭时控制器拒绝 `MOV`，回错误码 5），恢复走 `SVO 1` 原地保持。
  - 例外：当前位置越出标定行程（`TMN?`/`TMX?`）时，`SVO 1` 写入的目标被控制器夹到行程端点，轴会走回范围内（实测：开环漂到 −4.04 µm 后开伺服回到 0）。**连接/重连动作本身即可能触发**，不限于用户从「释放」恢复。
- 预留 CCD / 外部设备联动接口：每点到位后调用，图像路径写回该点元数据。

## 技术栈

| 层 | 选型 |
|---|---|
| 后端 | Python 3.12+ + FastAPI + uvicorn（`--workers 1`） |
| 硬件 API | PIPython。Windows：GCS DLL（`ConnectUSB`）；Linux：FTDI 虚拟串口（`PISerial`，115200） |
| 设备访问 | 专用 owner 线程 + `queue.Queue`（不用 `asyncio.to_thread`） |
| 命令通道 | REST + Pydantic 校验 |
| 遥测通道 | SSE（不用 WebSocket） |
| 前端 | 单页原生 HTML/JS，零构建 |
| 曲线 | uPlot |
| 存储 | 每点元数据 SQLite（WAL）；曲线 npz；图像由 CCD 库落盘 |
| 监听地址 | `127.0.0.1` |

不用：Streamlit、NiceGUI、Flask、React/Vue/Vite、Django、WebSocket、Redis/Celery/PostgreSQL。

## 结构

    backend/     Python 后端，只允许上层依赖下层：
                 server（路由/SSE） → scanner（扫描执行器） → store（SQLite）
                 → ccd（采图接口） → pi_stage（设备层，唯一 owner 线程） → config
    frontend/    单页原生 HTML/JS，零构建
    tools/       上机自检脚本
    docs/        PI 手册、GCS 命令表
    data/        运行期生成（SQLite + 图像），不入库

## 编码规范

高效、高质量、少防御性编程、少补丁。

> 例外：进硬件前的参数校验、软限位夹取、设备错误码检查是安全边界，必须保留。
