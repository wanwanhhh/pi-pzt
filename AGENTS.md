# AGENTS.md

## 规则

1. 未经用户允许，不得创建子代理（subagent）。
2. **未经用户允许，不得执行 git 提交、推送、reset 等会改动历史的操作。** 改文件、跑测试、上机自检不用问。

## 需求

### 通用

- 单人单机使用，界面中文。
- **跨平台**：同时兼容本机 Linux（Ubuntu 24.04）与 Windows。
- **后端负责控制**：硬件访问、参数校验、限位、安全逻辑、状态机全部在后端。
- **前端只负责显示与输入，不承担后端功能**：不复制业务逻辑、不做本地权威状态、不自己判断限位；前端被绕过或篡改也不影响设备安全。
- 三个动作：**手动（点到点）**、**自动扫描（等间隔序列）**、**每点配合 CCD 采图**。
  - 自动主流程：**移动 → 停稳 → 采图**，逐点进行。位置只作为每点元数据记录，**不做 10 kHz 高速采集通路**。
  - 任务由后端执行器运行，**独立于浏览器连接**（关页面、断网都继续跑）。
- 扫描控制：**暂停 / 继续 / 中止** 三个独立动作。
- 扫描默认**单向逼近**：往返走会因方向性迟滞导致图像错位。
- **位置精度是台子的属性，后端不承诺**：后端只保证「命令了什么」，保证不了「实际走了多少」。
  扫描的「等间隔」是按命令值和读数说的，**不是按真值说的**。
- **每点采图前按步距再校验一次偏差**：超过半个步距就判该点无效（不采图、不入库、中止扫描）。
  设备层的到达容差是**绝对**的，步距比它小时那道校验会失效（丢帧恰好差一个步距，会落进容差里）；
  这条是**相对**的，与设备无关。
- **读数和闭环用的是同一个传感器**：两者自洽，所以「停稳 / 到位」判据可靠；
  但传感器自身的标定误差（增益、零点）它报不出来。**绝对精度要靠外部标定**（干涉仪、显微镜刻度），不在本仓库范围内。
- 预留 CCD / 外部设备联动接口：每点到位后调用，图像路径写回该点元数据。
- **只做闭环**，不提供开环/电压模式；开环调试（频响、陷波滤波器）用厂家上位机软件。
- **停稳判据是「台子不动了」，不是「到位精度」**：连续几次读数不再变化就可以采图。
  门限只由读数噪声（连接时实测）和扫描步距决定，**与相机、光学分辨率无关**。
  - 已知限制：台子在蠕变时读数变化极慢，会落进 ε 内被判成「停稳」。这是这个判据的固有性质，
    **接受它，不当 bug 查**。
  - **判据的鲁棒性来自采样间隔抖动与窗口长度，不来自采样率**：固定 50 ms 是 20 Hz 均匀网格，
    闭环若在 20 Hz 附近振荡，拍频极低，连续几次读数会看起来纹丝不动。提高采样率不解这个问题。
- 因此**不需要高实时采样**：慢速轮询足够，设备的高速推送能力用不上（它只值得用来做表征）。
- 停稳判据由**设备层**实现，上层只处理「到位 / 超时 / 中止」，不关心底层怎么判定。
- 「停止」与「释放」是两个独立动作：**停止** = 停在当前位置；**释放** = 卸力（异常振动时用）。两者能做到什么程度取决于设备。

### PI E-709

- **停稳判据由控制器给出**：到位信号置位后，再加一段固定延时（默认 100 ms，界面可调）。
- **方向性迟滞实测约 0.1 µm**，扫描必须单向逼近。
- **「停止」能立刻停住并保持位置**，伺服不动、不漂移。
- **「释放」能卸掉输出**（异常振动时用）。释放后控制器拒绝运动，恢复时原地保持。
  - 例外：若当前位置已在标定行程之外，恢复动作会把它拉回行程内。**连接/重连本身即可能触发**，不限于用户从「释放」恢复。
- 行程上下限由控制器标定给出，是软限位的权威来源。

### XMT E53.D1S-H

- **仅 Windows**，不做 Linux 分支。
- **单位陷阱（实测，必踩）**：设点 `B3=1` 与行程 `27/35` 都是 **µm**，但**读回 `6`/`8` 是 4/3 µm**
  （×0.75 才是 µm）。`53` 只回一个「位移」，**不含尺度**，手册全书没写这个系数。
  **把读回值直接当设点写回去，台子会跑偏 4/3 倍** —— 实测当时读回 144.8785，原样当设点写回去后
  读回变成 193.2605，按 ×0.75 折算真实位置从 108.66 µm 走到 144.95 µm（多走 36.3 µm），而本意是零位移。
  （两个读回值是量到的，108.66 / 144.95 是折算出来的。）
  设备层必须在边界上折算，绝不让读回值流进设点。
- **设备不提供到位信号**，停稳判定必须由后端软件自己做。
  - 实测：静止噪声 σ 0.040 µm（单次）／0.022 µm（6 次均值）；**长时间静止不漂移**
    （1 ms 推送抓 5 s，分段均值只差 0.007 µm）。定位误差 ±0.045 µm；**刚走完的 2 s 内**
    蠕变 +0.025 µm（这一条是别把刚到位当成稳了的理由，不是长期漂移）。
    所以门限压到 0.1 µm 没问题 —— 和 PI 那套「连续几次读数不变」不同，
    探针足够密时应当**开窗取均值**再判稳定。
  - **扫描路径上的误差比上面那个 ±0.045 µm 大**（实测 0.02~0.12 µm），拆开是三样东西：
    **单次读数噪声 σ 0.030~0.035 µm**（与动不动无关：静止、刚走完、走完 4 分钟都是这个数）；
    **一处稳定的偏置**（设点 22.0 µm：读数均值停在 +0.072~0.079 µm，连续 220 s 不回落也不收敛，
    所以**不是「还没到位」**，也不是蠕变尾巴）；加上噪声叠出来的极值（p-p 0.19 µm）。
    偏置随区域变号：10~20 µm 段 +0.064 µm（11 个点全为正）、145~147 µm 段 −0.044 µm ——
    它在闭环内部（设点链与读回链对不上一个问题），后端只能读出来；**拿常数去补是错的**。
  - **读回值有 3.25 nm 的量子**（原始报文实测：只落在 0.00433 个 4/3 µm 单位的整数倍上）。
    远小于噪声，不是偏差的来源；但也就分辨不了 3 nm 以下的位移。
  - 所以**到达容差不能按 ±0.045 取**（会大量假失败）——设备层取 0.2 µm，
    而且**扫描步距必须远大于「偏置 + 噪声」**：步距 1 µm 时相对校验阈值 0.5 µm、
    实测最坏 0.12 µm，有 4 倍余量；步距压到 0.2 µm 就会被它们随机打掉点。
- **设备没有「停止运动」和「关伺服」能力**，这两个动作只能这样表达：
  - 「停止」= 不再下发新目标；**已在途的长行程会走完**，拦不住。
  - 「释放」= 切到开环并把输出写到零（卸力）；**不保证停在原位**，界面文案用「切至开环（卸力）」。
- **设点无应答，丢帧是静默的**：每点下发后必须读回校验，超差即**中止扫描 + 该点标无效 + 不采图**。
- **没有速度指令**（协议表里没有对应的 B3）：`set_velocity` 明确拒绝并报错，不假装接受 ——
  速度由闭环自身决定，界面上的速度框对它没有意义。
- **能力声明（caps）随状态一起下发给界面，界面据此分叉文案与可用性**：没有速度指令的设备
  速度框直接禁用；停止/急停/释放的说明按 `has_stop_command` / `release_mode` 改写；
  `/api/stop` 返回 soft 时提示语是「软停：不再下发新目标（在途行程拦不住）」。
  **caps 只影响文案与可用性，绝不参与夹取与安全逻辑** —— 真正的拒绝永远在后端。
- **位置单位与行程上下限必须连接时从设备读回**，不得写死；单位在设备层折算成 µm 后再上传。
- **方向性迟滞未实测**：`xmt_move.py` 量的 0.029 µm 是**同向**两次到达之差（两次都向上逼近，
  只是起点距离不同），不是迟滞；反向逼近要另测。不得沿用 PI 的 0.1 µm。**单向逼近仍是默认。**
- **手册的几条限制真机上更松**（实测）：帧间隔 50 ms **不强制**，背靠背连发两条都会被处理；
  周期推送 `B3=8` 能跑到 **1 ms**；**流运行中可以下发并执行其它命令**。
  - 这只用于**表征**。生产端（扫描、遥测）仍按手册留帧间隔、不并发下发，
    也不拿 1 ms 推送当停稳判据 —— 那几条限制是下位机的行为边界，不是许可证。
- **停稳时间是旋钮位置，不是设备常量**：后面板有「闭环积分调节电位器」（调阶跃响应）和
  「低通滤波调节」（调闭环响应时间）。写死的停稳参数只对当前旋钮位置成立，重调就得重测。

## 技术栈

| 层             | 选型                                                         |
| -------------- | ------------------------------------------------------------ |
| 后端           | Python 3.12+ + FastAPI + uvicorn（`--workers 1`）             |
| 硬件 API（PI）  | PIPython。Windows 走 GCS DLL（`ConnectUSB`），Linux 走 FTDI 虚拟串口（`PISerial`，115200） |
| 硬件 API（XMT） | 芯明天 E53.D1S-H：**自写协议 + pyserial** 直连 USB CDC 虚拟串口（`VID_0483&PID_0002`，系统自带 `usbser.sys`，无需厂家驱动）。**仅 Windows**；协议与资料见 `docs/xmt/README.md` |
| 设备访问       | 专用 owner 线程 + `queue.Queue`（不用 `asyncio.to_thread`）    |
| 命令通道       | REST + Pydantic 校验                                          |
| 遥测通道       | SSE（不用 WebSocket）                                         |
| 前端           | 单页原生 HTML/JS，零构建                                       |
| 曲线           | uPlot                                                        |
| 存储           | 每点元数据 SQLite（WAL）；曲线 npz；图像由 CCD 库落盘            |
| 监听地址       | `127.0.0.1`                                                  |

不用：Streamlit、NiceGUI、Flask、React/Vue/Vite、Django、WebSocket、Redis/Celery/PostgreSQL。

## 结构

    backend/     Python 后端，只允许上层依赖下层：
                 server（路由/SSE） → scanner（扫描执行器） → store（SQLite） → ccd（采图接口）
                 → stage_api（设备层公共面：能力声明 + 状态结构 + 契约）
                     → pi_stage（PI E-709）
                     → xmt_stage（芯明天 E53.D1S-H）   ← 配置里选一台，同时只激活一台
                 → config（全部可调参数）
                 tests/  离线单测，不碰硬件
    frontend/    单页原生 HTML/JS，零构建
    tools/       上机自检脚本
    docs/        PI 手册、GCS 命令表；docs/xmt/ 芯明天 E53.D1S-H 资料（协议包 + 手册）
    data/        运行期生成（SQLite + 图像），不入库

## 编码规范

高效、高质量、少防御性编程、少补丁。

> 例外：进硬件前的参数校验、软限位夹取、设备错误码检查是安全边界，必须保留。
