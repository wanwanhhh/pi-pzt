# AGENTS.md

## 规则

1. 未经用户允许，不得创建子代理（subagent）。
2. **未经用户允许，不得执行 git 提交、推送、reset 等会改动历史的操作。** 改文件、跑测试、上机自检不用问。
3. **问答条目只有用户明确同意才能写进 `docs/问答.md`** —— 不许自己往里加，也不许「顺手记一条」。
   条目**按需求方 / 用户的角度解释，不按专业角度解释**：先说这对使用者意味着什么，
   不从器件原理、协议时序这类地方切入；**尽量用表格**，能列成「情况 / 表现 / 怎么办」的就不写段落。

## 需求

> **本文件只写「要做到什么」**：规则、需求、选型、结构、规范。
> 装置细节、实测数字、踩坑经验写在各设备自己的文档里 —— **动设备层代码前先读对应那一份**：
> - PI E-709.CRG + P-621.1CD → `docs/pi/设备认识账.xml`（接入与安装见根 `README.md`）
> - 芯明天 E53.D1S-H + XD780.200S → `docs/xmt/设备认识账.xml`（协议与厂家资料见 `docs/xmt/README.md`）
> - 索雷博 CS165MU（Zelux）相机 → `docs/thorlabs/设备认识账.xml`（SDK 安装见根 `README.md`）
> 本文件不重复设备文档里的数字；**设备文档里标为「处置」的条目是硬要求**。

### 通用

- 单人单机使用，界面中文。
- **跨平台**：同时兼容本机 Linux（Ubuntu 24.04）与 Windows。
- **后端负责控制**：硬件访问、参数校验、限位、安全逻辑、状态机全部在后端。
- **前端只负责显示与输入，不承担后端功能**：不复制业务逻辑、不做本地权威状态、不自己判断限位；前端被绕过或篡改也不影响设备安全。
- 界面按设备分**三页**：**对准**（位置读数 / 手动控制 / 位置曲线）、**扫描**（参数与进度 / 点位 / 历史）、
  **预览**（相机预览 / 手动保存的原始帧图库）。
  这么分是因为后端本来就规定它们互斥（扫描占着设备时手动操作会被 409 拒，相机也归扫描用）——
  藏起来的是「此刻用不了的东西」，不是「还有用的东西」。
  **分页是显示状态，不是权限**：不许拿"现在在哪一页"当防误操作的手段，真正的拒绝永远在后端。
  视图名写进 URL hash（刷新、开两个标签各停一页都靠它）。
- 三个动作：**手动（点到点）**、**自动扫描（等间隔序列）**、**每点配合 CCD 采图**。
  - 自动主流程：**移动 → 停稳 → 采图**，逐点进行。位置只作为每点元数据记录，**不做 10 kHz 高速采集通路**。
  - 任务由后端执行器运行，**独立于浏览器连接**（关页面、断网都继续跑）。
- 扫描控制：**暂停 / 继续 / 中止** 三个独立动作。
- 扫描默认**单向逼近**：往返走会因方向性迟滞导致图像错位。
- **位置精度是台子的属性，后端不承诺**：后端只保证「命令了什么」，保证不了「实际走了多少」。
  扫描的「等间隔」是按命令值和读数说的，**不是按真值说的**。
- **每点采图前按步距再校验一次偏差**：超过半个步距就判该点无效（不采图、不入库、中止扫描）。
  设备层的到达容差是**绝对**的，步距比它小时那道校验会失效（丢帧恰好差一个步距，会落进容差里）；
  这条是**相对**的，与设备无关。
- **读数和闭环用的是同一个传感器**：两者自洽，所以「停稳 / 到位」判据可靠；
  但传感器自身的标定误差（增益、零点）它报不出来。**绝对精度要靠外部标定**（干涉仪、显微镜刻度），不在本仓库范围内。
- 预留 CCD / 外部设备联动接口：每点到位后调用，图像路径写回该点元数据。
- **只做闭环**，不提供开环/电压模式；开环调试（频响、陷波滤波器）用厂家上位机软件。
- **停稳判据是「台子不动了」，不是「到位精度」**：连续几次读数不再变化就可以采图。
  门限只由读数噪声（连接时实测）和扫描步距决定，**与相机、光学分辨率无关**。
  - 已知限制：台子在蠕变时读数变化极慢，会落进 ε 内被判成「停稳」。这是这个判据的固有性质，
    **接受它，不当 bug 查**。
  - **判据的鲁棒性来自采样间隔抖动与窗口长度，不来自采样率** —— 提高采样率解决不了拍频；
    参数与机理见 `backend/config.py` 的停稳一节与设备文档。
- 因此**不需要高实时采样**：慢速轮询足够，设备的高速推送能力用不上（它只值得用来做表征）。
- **位置曲线（看稳定性）取自遥测轮询的环形缓冲，不额外轮询设备**：设备带宽只够一个轮询者，
  曲线与界面上的读数因此永远同源。取窗口用的是服务器时刻的绝对两端（记录起点 = 上一帧遥测的 ts），
  所以浏览器切到后台、最后一次取数迟到一分钟，拿回来的仍是原来那一段，点数按实采算、不插值。
  标准差在前端按窗口内的实采点算（显示统计不是业务逻辑），**点数与平均间隔要一并显示出来** ——
  曲线反映的是采样率下的那一段，不是设备带宽下的全部。
- 停稳判据由**设备层**实现，上层只处理「到位 / 超时 / 中止」，不关心底层怎么判定。
- 「停止」与「释放」是两个独立动作：**停止** = 停在当前位置；**释放** = 卸力（异常振动时用）。两者能做到什么程度取决于设备。

### 设备层共同要求

- **行程与安全边界以设备文档里的实测可用区间为准**。设备自报的限位要不要采信、按哪条夹取，
  一律看设备文档：**没有标定数据的设备（XMT）自报值不可用**，只能当参考区间；
  有标定数据的设备（PI）自报值就是权威来源。
- **折算系数不能长期依赖写死的常数**：读回值 → µm 之类的系数，设备一旦被重新标定，
  读数就再也落不到目标附近 —— **兜底的是到达容差**（移动显示未到位、扫描判该点无效并中止）。
  设备层再加一层**诊断**：判稳却离目标很远时，把「像丢帧」还是「像系数变了」说清楚。
  **诊断不拦截运动** —— 拦截会多一个误判源（丢帧时台子停在上一目标，算比值必然算错）。
  **也不要在连接时动台子。**
- **能力声明（caps）随状态一起下发给界面**，界面据此分叉文案与可用性：没有速度指令的设备
  禁用速度框；停止 / 急停 / 释放的说明按实际能力改写；`/api/stop` 返回 soft 时提示语是
  「软停：不再下发新目标（在途行程拦不住）」。
  **caps 只影响文案与可用性，绝不参与夹取与安全逻辑** —— 真正的拒绝永远在后端。
- **停稳参数不是设备常量**：改了设备的整定（旋钮、滤波器、PID）就得重测。
- 每台设备的**停止 / 释放 / 行程 / 停稳**细节按各自文档如实实现，不许拿另一台设备的结论顶上。

### PI E-709

- **停稳判据由控制器给出**：到位信号 + 一段固定延时（延时可调）。
- **扫描必须单向逼近**（方向性迟滞见设备文档）。
- 停止 / 释放两个动作按设备文档的语义实现，不许一律写成「保持伺服」。
- 细节与实测数字见 `docs/pi/设备认识账.xml`。

### XMT E53.D1S-H

- **仅 Windows**，不做 Linux 分支。
- **读回值与设点单位不同**（设点 µm、读回是另一套单位；系数与来历见设备文档）：设备层必须在边界折算，
  **绝不让读回值流进设点**；折算系数按「设备层共同要求」只做诊断，不拦截运动。
- **设备不提供到位信号**：停稳判据必须由后端软件自己做 —— 门限由连接时实测的读数噪声定，
  探针足够密时开窗取均值再判稳定；判据在设备层实现，上层只处理「到位 / 超时 / 中止」。
- **设点无应答，丢帧是静默的**：每点下发后必须读回校验，超差即**中止扫描 + 该点标无效 + 不采图**。
- **设备没有「停止运动」和「关伺服」能力**：这两个动作只能按设备文档的语义表达，界面文案要说实话。
- **没有速度指令**：`set_velocity` 明确拒绝并报错，不假装接受；界面速度框禁用。
- **可用行程不是设备自报的行程**：按设备文档里的实测可用区间夹取（运行上限见该文档）。
- 细节、实测数字与踩坑经验见 `docs/xmt/设备认识账.xml`。

### 索雷博 CS165MU（相机）

- **仅 Windows**，走厂家 Python SDK（`thorlabs_tsi_sdk`，**不在 PyPI 上**，用 SDK 包里的本地包装）。
- **曝光由预览与采图共用同一个值**，而且随时可改（预览流不用停）；**增益恒为 0**，改增益的接口一律拒绝。
  理由在设备文档 A4/A5：增益只把信号和噪声一起放大，信噪比不会变好；亮度只用曝光调。
- **手动保存的帧 = 原生全幅、16 位灰度、不裁剪、不做任何拉伸或伪彩**。
  曝光与**质心**都写进 PNG 自己的 `tEXt` 块（键 `ExposureUs`、`CentroidPx`，后者是传感器坐标），
  **图像自证用了哪次参数、亮心在哪**，不依赖本仓库的库；图库那一行就是读它显示的。
  老图没有这两个块就写"未记录"，**不许猜一个值出来**。
- **图库只列手动保存的帧**（按库里的登记表，不靠文件名前缀猜）；扫描各点的图归该扫描的点位元数据。
  改名 = **真的改磁盘文件名**，库记录同步改，失败回滚。
- **预览上显示质心**，口径是**整幅图的强度加权重心**（cx = ΣI·x/ΣI）：**不扣背景、不设阈值、不开窗**。
  在后端用**原生 16 位帧（未旋转的那一帧）**算完再下发，前端只画十字线和数字（显示，不是计算）。
  所以它是"整幅亮度重心"，背景也照权重参与 —— 光斑占总强度越小，它离光斑越远
  （实测数字见 `docs/thorlabs/设备认识账.xml` 的处置清单）。
  饱和按**实测满量程**判（`TL_SATURATION_ADU`，数值见设备文档），峰值到顶就在界面上标出来。
- **预览可以转朝向**（0/90/180/270，顺时针），但**只转预览**：保存的原生帧永远是**传感器朝向**
  —— 转向是"看的方向"，不该进文件，也不该让像素坐标和传感器脱钩。
  90° 整数倍必须是**精确置换、不重采样**（实现见 `backend/thorlabs_ccd.py`）。
- **质心读数永远是传感器坐标**（与保存的 PNG 同一套），**不受预览朝向影响** —— 拿它去对文件里的像素
  不用做任何换算。十字线要画在"看到的那一帧"上，所以由**前端**按朝向换算（那是显示，不是计算）；
  换算表钉在 `backend/tests/test_centroid.py` 与 `tools/uitest.js` 两边。
- **点预览图取轮廓图**：过该点的**整行**（水平）与**整列**（垂直）两条**全长**剖面，
  **取自原生 16 位帧**（不是预览 JPEG），**原值上图** —— 不平滑、不扣背景、不归一化、不算峰位/FWHM。
  坐标是**显示坐标**：先按当前朝向转成视图再切，所以转了 90° 之后"水平"仍是你眼睛看到的水平
  （保存的 PNG 不转，那条路读的是文件坐标）。预览页**实时刷新**；图库大图点一下切的是**保存的那个 PNG**。
- **相机与位移台互斥**：扫描进行中相机接口一律 409；**开始扫描前后端自己先关预览**，不靠界面先点一下。
- **扫描中相机出错 = 整条扫描就地 failed**（不重试、不"该点判无效后继续"、不自动清理半截扫描）。
  跑过的点都在库里，**那条半截扫描由人在界面上手动删除**；理由见设备文档的处置清单。
- 细节、实测数字与踩坑经验见 `docs/thorlabs/设备认识账.xml`。

## 技术栈

| 层             | 选型                                                         |
| -------------- | ------------------------------------------------------------ |
| 后端           | Python 3.12+ + FastAPI + uvicorn（`--workers 1`）             |
| 硬件 API（PI）  | PIPython。Windows 走 GCS DLL（`ConnectUSB`），Linux 走 FTDI 虚拟串口（`PISerial`，115200） |
| 硬件 API（XMT） | 芯明天 E53.D1S-H：**自写协议 + pyserial** 直连 USB CDC 虚拟串口（`VID_0483&PID_0002`，系统自带 `usbser.sys`，无需厂家驱动）。**仅 Windows**；协议与资料见 `docs/xmt/README.md`，实测与经验见 `docs/xmt/设备认识账.xml` |
| 硬件 API（相机）| 索雷博 CS165MU：`thorlabs_tsi_sdk`（厂家 SDK 本地包，ctypes 调原生 DLL；19 个 DLL 按全路径预加载）。**仅 Windows**；实测与踩坑见 `docs/thorlabs/设备认识账.xml` |
| 设备访问       | 专用 owner 线程 + `queue.Queue`（不用 `asyncio.to_thread`）    |
| 命令通道       | REST + Pydantic 校验                                          |
| 遥测通道       | SSE（不用 WebSocket）                                         |
| 前端           | 单页原生 HTML/JS，零构建                                       |
| 曲线           | uPlot                                                        |
| 存储           | 每点元数据 SQLite（WAL）；曲线 npz；图像由 CCD 库落盘            |
| 监听地址       | `127.0.0.1`                                                  |

不用：Streamlit、NiceGUI、Flask、React/Vue/Vite、Django、WebSocket、Redis/Celery/PostgreSQL。

## 结构

    backend/     Python 后端，只允许上层依赖下层：
                 server（路由/SSE） → scanner（扫描执行器） → store（SQLite） → ccd（采图接口）
                     → thorlabs_ccd（索雷博 CS165MU：owner 线程 + 原生全幅 16 位 PNG）
                 → stage_api（设备层公共面：能力声明 + 状态结构 + 契约）
                     → pi_stage（PI E-709）
                     → xmt_stage（芯明天 E53.D1S-H）   ← 配置里选一台，同时只激活一台
                 → config（全部可调参数）
                 trace（位置曲线环形缓冲：遥测线程写、HTTP 只读，不额外轮询设备）
                 tests/  离线单测，不碰硬件
    frontend/    单页原生 HTML/JS，零构建
    tools/       上机自检脚本
    docs/        PI 手册与 GCS 命令表 + docs/pi/设备认识账.xml
                 docs/xmt/ 芯明天 E53.D1S-H 资料（协议包 + 手册）+ docs/xmt/设备认识账.xml
                 docs/thorlabs/ 索雷博相机 + docs/thorlabs/设备认识账.xml
                 （每台设备一份「认识账」：已证实 / 未证实 / 已作废 / 经验与坑 / 对后端的处置清单）
                 docs/问答.md  用户问过的项目级问答（收录需用户明确同意）
    data/        运行期生成（SQLite + 图像），不入库

## 编码规范

高效、高质量、少防御性编程、少补丁。

> 例外：进硬件前的参数校验、软限位夹取、设备错误码检查是安全边界，必须保留。
